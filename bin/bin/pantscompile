#!/bin/bash
findbuilds() {
  cs=$(echo $1 | tr / ' ')
  d='.'
  for c in $cs; do
    d=$d/$c
    if [ -f "${d}/BUILD" ]; then
      echo "${d}/BUILD" 2>/dev/null
    fi
  done
}

file=$1
rootbuild=$(findbuilds $file | head -1)
rootdir=$(dirname $rootbuild)

./pants goal test $rootdir:: | nocolor
