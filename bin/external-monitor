#!/bin/sh
export DISPLAY=:0.0
export XAUTHORITY=/home/edma2/.Xauthority

connect(){
  xrandr --output VGA1 --auto --output LVDS1 --off
}

disconnect(){
  xrandr --output VGA1 --off --output LVDS1 --auto
}

xrandr | grep "VGA1 connected" &> /dev/null && connect || disconnect
